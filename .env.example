# Grafana Configuration
GRAFANA_ADMIN_PASSWORD=change-this-secure-password

# Optional: Additional Grafana plugins (comma-separated)
# GRAFANA_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource

# Network Configuration
# If you need to customize the network name, you can do it in docker-compose.yml
